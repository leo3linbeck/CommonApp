exports.createNewUser = function createNewUser (fullname, username, password) {	var newUser = directory.addUser(username, password, fullname);	newUser.putInto('Applicants');	directory.save();		return newUser;};exports.findUser = function findUser (username) {	return directory.user(username);};exports.bindUserToAddress = function bindUserToAddress(familyName, familyID, userID) {	var u = directory.user(userID);	var f = ds.Family(familyID);	f.name = familyName;	f.userID = userID;	f.save();		return ( { family: f, user: u } );}